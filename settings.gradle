pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}

rootProject.name='HyperLauncher'
include ':app_pojavlauncher'
project(':app_pojavlauncher').projectDir = new File('app_pojavlauncher')

include ':forge_installer'
include ':dnbglfw'
