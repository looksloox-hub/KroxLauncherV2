pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}

rootProject.name='HyperLauncher'
include ':hyperX_launcher'
project(':hyperX_launcher').projectDir = new File('app_pojavlauncher')

include ':forge_installer'
include ':dnbglfw'
